## Purpose: Helper functions for SNA for Startups R code
## Author: Bryan Shepherd
## Email: bryan@programmingr.com

# Load packages. Install if not already installed.
if(!suppressWarnings(require(igraph))) {
  install.packages("igraph")
  library(igraph)
} 
